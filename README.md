# Auralizer
A shader based audio visualizer written in C++ using OpenGL.

## Build
Currently builds are Windows only.

1. Clone the repository using the `--recursive` flag.
2. Generate build files using CMake.
3. Build using your preferred system/IDE.
