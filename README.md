# Auralizer
A shader based audio visualizer written in C++ using OpenGL.

## Build
Currently builds are Windows only.

1. Clone the repository using the `--recursive` flag.
2. Generate build files using CMake.
3. Build using your preferred system/IDE.

## About licensing
The repository uses the MIT license with the "fonts" and "libs" directories excluded, which have their own separate licenses.
Information about which licenses each third party libraries or fonts use can be found within their respective directories.
